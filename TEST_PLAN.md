# 任务与日程中心测试计划

## 目标与范围

验证本地任务与日程中心的领域规则、REST API、浏览器主流程、持久化、双语和窄屏可用性。覆盖任务 CRUD、完成状态、优先级与日期筛选；日程 CRUD、日期筛选、时间校验与冲突提示；以及 JARVIS 创建和查询工具的数据契约。

不覆盖第三方日历同步、重复日程、系统通知、拖拽、多用户权限、真实 DeepSeek 响应和真实语音合成。

## 环境与测试数据

- Node.js 20+，Vitest、Supertest、Playwright，桌面 Chrome。
- API 测试使用 `.test-data`，E2E 使用 `.e2e-data`，测试前清空，不读写正式 `data`。
- 时间数据使用带时区 ISO 字符串；浏览器断言使用本地 `datetime-local` 控件。

## 用例与自动化映射

| 编号 | 场景 | 层级 | 预期 |
| --- | --- | --- | --- |
| UT-01 | 任务必填、状态、优先级、ISO 时间校验 | 单元 | 非法输入被拒绝 |
| UT-02 | 日程起止时间校验 | 单元 | 结束必须晚于开始 |
| UT-03 | 重叠、相邻和编辑自身 | 单元 | 重叠命中；相邻/自身不冲突 |
| UT-04 | 日期/状态/优先级筛选与月历生成 | 单元 | 筛选准确，月历为周一开始的 42 格 |
| UT-05 | ISO 与本地输入转换 | 单元 | 本地控件往返不偏移 |
| API-01 | 任务创建、查询、完成、删除及持久化 | API | 状态码与资源内容正确 |
| API-02 | 任务空标题、非法枚举/时间、不存在资源 | API | 返回 400 或 404 |
| API-03 | 日程创建、日期查询和冲突返回 | API | 成功保存并返回冲突数组 |
| API-04 | 日程非法区间及不存在资源 | API | 返回 400 或 404 |
| E2E-01 | UI 创建、完成、筛选、编辑、删除任务 | E2E | 列表即时更新且删除需确认 |
| E2E-02 | 创建重叠日程并刷新 | E2E | 显示冲突提示，刷新后数据保留 |
| E2E-03 | 英文切换和 390px 窄屏 | E2E | 标题翻译且主要操作可见 |

## 风险与通过标准

- 浏览器时区差异通过 ISO 存储和本地控件往返测试控制。
- JSON 文件并发写入适用于当前单用户原型；多进程写入不在首版保证范围。
- 通过标准：`npm run build`、`npm test`、`npm run test:e2e` 全部成功；无中心功能阻塞或高严重度缺陷。

## 执行结果

- 2026-08-09：`npm run build` 通过。
- 2026-08-09：单元测试 5/5 通过，API 测试 4/4 通过。
- 2026-08-09：Chrome E2E 测试 3/3 通过，耗时约 14 秒。
- 修复记录：冲突弹窗测试支持中英文；E2E 测试服务器由全局生命周期启动和关闭，消除 Windows 下残留进程和套件超时。
- 已知边界：JSON 文件存储面向单用户单进程原型，不保证多进程并发写入一致性。
